# LLM-генерация имен файлов и заголовков

## 🎯 Проблема
Предыдущая система использовала предопределенный набор ключевых слов для генерации имен файлов, что ограничивало ее только AWS-сервисами и известными архитектурными паттернами.

## ✨ Решение
Теперь система использует **LLM (Claude)** для генерации умных имен файлов и заголовков на основе **любого** запроса пользователя.

## 🔧 Как это работает

### 1. LLM-генератор имен
```python
@tool
def generate_filename_and_title(user_query: str) -> str:
    """Генерирует имя файла и заголовок с помощью LLM"""
```

### 2. Умный промпт
LLM получает четкие инструкции:
- **Имя файла**: английский, только буквы/цифры/подчеркивания, до 40 символов
- **Заголовок**: русский, красивый, отражает суть архитектуры
- **Формат ответа**: строгий JSON

### 3. Интеграция с диаграммами
```python
# Генерируем имя файла и заголовок с помощью LLM
naming_result = generate_filename_and_title(context)
naming_data = json.loads(naming_result)

filename = naming_data['filename']
title = naming_data['title']
```

## 📁 Созданные файлы

### Основные файлы:
- `llm_dynamic_naming.py` - полная реализация с LLM
- `aws_architect_final.py` - обновленная основная версия
- `demo_llm_naming.py` - демонстрация возможностей
- `test_llm_naming.py` - тестирование LLM-генерации

## 🎨 Примеры генерации

| Запрос пользователя | Имя файла | Заголовок |
|-------------------|-----------|-----------|
| "Создай систему мониторинга IoT в умном городе" | `iot_smart_city_monitoring` | "Система мониторинга IoT в умном городе" |
| "Платформа онлайн обучения с видео" | `online_learning_video_platform` | "Платформа онлайн обучения с видеостримингом" |
| "Анализ финансовых рисков в реальном времени" | `realtime_financial_risk_analysis` | "Система анализа финансовых рисков в реальном времени" |

## 🚀 Преимущества

### ✅ Универсальность
- Работает с **любыми** темами
- Не ограничен предопределенными ключевыми словами
- Адаптируется к контексту

### ✅ Качество
- Осмысленные имена файлов
- Красивые заголовки на русском
- Соблюдение технических требований

### ✅ Надежность
- Fallback механизмы при ошибках
- Очистка имен файлов от недопустимых символов
- Ограничение длины

## 🔄 Workflow

1. **Пользователь** отправляет запрос любой тематики
2. **LLM** анализирует запрос и генерирует имя файла + заголовок
3. **Система** создает диаграмму с сгенерированным именем
4. **Агент** сохраняет ответ в markdown с тем же именем

## 💡 Технические детали

### Промпт для LLM:
```
Проанализируй запрос пользователя и создай:
1. Имя файла - короткое, описательное, английский, только буквы/цифры/подчеркивания
2. Заголовок - красивый заголовок на русском языке

Верни результат ТОЛЬКО в формате JSON:
{"filename": "имя_файла", "title": "Заголовок документа"}
```

### Обработка ответа:
- Извлечение JSON из ответа LLM
- Очистка имени файла от недопустимых символов
- Fallback при ошибках парсинга

## 🎯 Результат

Теперь система может создавать осмысленные имена файлов и заголовки для **любых** тем:
- IoT и умные города
- Образовательные платформы
- Финансовые системы
- Медицинские решения
- Игровые платформы
- E-commerce
- Социальные сети
- И многое другое!

**LLM делает систему по-настоящему универсальной!** 🚀